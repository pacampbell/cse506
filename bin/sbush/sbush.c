#include <stdio.h>
#include <stdlib.h>

#define INPUT_BUFFER 1024

int main(int argc, char *argv[], char* envp[]) {
    int running = 1;
    while(running) {
        printf("> ");
        break;
        //scanf()

    }
    return EXIT_SUCCESS;
}
